# FrankenAgent Lab - Environment Configuration Example
# Copy this file to .env.development for local development
# or .env.production for production deployment

# ============================================================================
# ENVIRONMENT SELECTION
# ============================================================================
# Options: development, production, test
# This determines which .env file to load and validation rules to apply
ENVIRONMENT=development

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Development (SQLite - no setup required):
DATABASE_URL=sqlite:///./tmp/frankenagent_dev.db

# Production (Supabase PostgreSQL):
# DATABASE_URL=postgresql://postgres:[password]@db.your-project.supabase.co:5432/postgres

# Local PostgreSQL (alternative):
# DATABASE_URL=postgresql://frankenagent:password@localhost:5432/frankenagent

# ============================================================================
# URLS
# ============================================================================
# Development:
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
OAUTH_REDIRECT_URI=http://localhost:8000/api/auth/callback

# Production (must use HTTPS):
# FRONTEND_URL=https://yourdomain.com
# BACKEND_URL=https://api.yourdomain.com
# OAUTH_REDIRECT_URI=https://yourdomain.com/api/auth/callback

# ============================================================================
# LLM PROVIDER API KEYS
# ============================================================================
# OpenAI - Get key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic - Get key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# ============================================================================
# OAUTH CREDENTIALS
# ============================================================================
# Google OAuth - Configure at: https://console.cloud.google.com/apis/credentials
# Make sure to add your redirect URI as authorized redirect URI
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth - Configure at: https://github.com/settings/developers
# Make sure to add your redirect URI as callback URL
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================================================
# SUPABASE CONFIGURATION (Production Only)
# ============================================================================
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Generate a secure random key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=dev-secret-key-change-in-production

# ============================================================================
# AGNO FRAMEWORK
# ============================================================================
# Enable debug mode (development only)
AGNO_DEBUG=true
LOG_LEVEL=DEBUG

# Production settings:
# AGNO_DEBUG=false
# LOG_LEVEL=INFO

# ============================================================================
# EMAIL CONFIGURATION
# ============================================================================
# Brevo (formerly Sendinblue) - Get key at: https://app.brevo.com/settings/keys/api
# Free tier: 300 emails/day
# Required for password reset emails
BREVO_API_KEY=your-brevo-api-key-here
FROM_EMAIL=noreply@frankenagent.dev
FROM_NAME=FrankenAgent Lab

# ============================================================================
# TOOL API KEYS (Optional)
# ============================================================================
# Tavily Search - Get key at: https://tavily.com/
TAVILY_API_KEY=tvly-your-tavily-key-here

# ============================================================================
# RUNTIME CONFIGURATION
# ============================================================================
BLUEPRINTS_DIR=./blueprints
API_HOST=0.0.0.0
API_PORT=8000

# ============================================================================
# CORS CONFIGURATION (Security Critical!)
# ============================================================================
# Comma-separated list of allowed origins for CORS
# Development:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# Production (NEVER use "*"):
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================================================
# GCP CONFIGURATION (Production Only)
# ============================================================================
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a

# ============================================================================
# REDIS CONFIGURATION (Production Only)
# ============================================================================
REDIS_HOST=10.0.0.3
REDIS_PORT=6379

# ============================================================================
# SSL VERIFICATION
# ============================================================================
# Automatically disabled in development, enabled in production
# Override only if needed:
# SSL_VERIFY=false

# ============================================================================
# VARIABLE REFERENCE
# ============================================================================
# 
# REQUIRED VARIABLES (All Environments):
# - ENVIRONMENT: Must be "development", "production", or "test"
# - DATABASE_URL: Database connection string
# - FRONTEND_URL: Frontend application URL
# - BACKEND_URL: Backend API URL
# - OAUTH_REDIRECT_URI: OAuth callback URL
# 
# REQUIRED VARIABLES (Production Only):
# - SUPABASE_URL: Supabase project URL
# - SUPABASE_ANON_KEY: Supabase anonymous key
# - SUPABASE_SERVICE_ROLE_KEY: Supabase service role key
# - JWT_SECRET_KEY: Must be changed from default
# 
# OPTIONAL VARIABLES (Recommended):
# - OPENAI_API_KEY: Required for OpenAI models
# - ANTHROPIC_API_KEY: Required for Anthropic models
# - GOOGLE_CLIENT_ID: Required for Google OAuth
# - GOOGLE_CLIENT_SECRET: Required for Google OAuth
# - GITHUB_CLIENT_ID: Required for GitHub OAuth
# - GITHUB_CLIENT_SECRET: Required for GitHub OAuth
# - TAVILY_API_KEY: Required for Tavily search tool
# 
# OPTIONAL VARIABLES (Configuration):
# - LOG_LEVEL: Logging level (DEBUG, INFO, WARNING, ERROR)
# - AGNO_DEBUG: Enable Agno debug mode (true/false)
# - BLUEPRINTS_DIR: Directory for blueprint files
# - API_HOST: API server host
# - API_PORT: API server port
# - SSL_VERIFY: Override SSL verification (not recommended)
# - ALLOWED_ORIGINS: CORS allowed origins
# - GCP_PROJECT_ID: Google Cloud project ID
# - GCP_REGION: Google Cloud region
# - GCP_ZONE: Google Cloud zone
# - REDIS_HOST: Redis server host
# - REDIS_PORT: Redis server port

# ============================================================================
# QUICK START
# ============================================================================
# 1. Copy this file:
#    cp .env.example .env.development
#
# 2. Fill in required values:
#    - OPENAI_API_KEY or ANTHROPIC_API_KEY (for agent execution)
#    - GOOGLE_CLIENT_ID/SECRET (for Google OAuth)
#    - GITHUB_CLIENT_ID/SECRET (for GitHub OAuth)
#
# 3. Start the server:
#    poetry run uvicorn frankenagent.api.server:app --reload
#
# 4. Open the UI:
#    http://localhost:8000/static/index.html
#
# For production deployment, see: PRODUCTION_DEPLOYMENT_GUIDE.md
